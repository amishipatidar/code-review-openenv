[project]
name = "code-review-openenv"
version = "0.1.0"
description = "OpenEnv Code Review Environment"
requires-python = ">=3.9"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "openenv-core>=0.2.0"
]

[project.scripts]
server = "server.app:main"